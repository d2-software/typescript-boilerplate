services:
#  App.middlewares.StatusGetMiddleware:
#    class: ../../middlewares/status/statusGet.middleware
#    arguments: ['@App.libs.logger']

  App.middlewares.AuthenticationMiddleware:
    class: ../../middlewares/common/authentication.middleware
    main: AuthenticationMiddleware
